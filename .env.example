# Google OAuth — download credentials.json from Google Cloud Console
# Token files (token.json, token_drive.json) are auto-generated on first run

# Firecrawl API key (for JD scraping) — get from https://firecrawl.dev
FIRECRAWL_API_KEY=

# Optional: explicit Python path (if not using system default)
# PYTHON_PATH=C:/Users/dimit/AppData/Local/Programs/Python/Python310/python.exe
